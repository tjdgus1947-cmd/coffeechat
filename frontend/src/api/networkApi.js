// frontend/src/api/networkApi.js

// MainView.jsx가 임시로 사용하는 파일입니다.
// API 호출 로직은 services/api.js에 있습니다.
export const fetchNetworkData = (menteeId) => {
    console.log(`[networkApi] Fetching network data for ${menteeId}`);
    return Promise.resolve(null); 
};
