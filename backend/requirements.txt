# Dependencies collected from imports. Pin versions with `pip freeze` when needed.
# (ASCII only: pip on Windows reads this file as cp949)
fastapi
uvicorn[standard]
pydantic[email]
python-multipart
python-dotenv
supabase>=2.8
sentence-transformers
torch
shapely
google-generativeai
tqdm
